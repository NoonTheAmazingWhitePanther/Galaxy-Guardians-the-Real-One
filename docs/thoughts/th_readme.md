# Thoughts

Personal notes — Noon's, not Claude's. Ideas, half-formed plans, "what if"
scribbles, things that aren't ready to be a `todo/` item or a `wiki/` page
yet.

Claude does not write here unless explicitly asked to. This folder is not
part of the AI session-summary cycle described in `rules.md`.

## Naming
Every file here starts with `th_`, lowercase — see `rules.md` §1 at the
repo root for the full convention.
